### Calendar Company Mock API

## Endpoints

* `/` 
* `/companies/:id/`

## Development instructions

### Manual

```shell
nvm install && node index.js
```


### via Docker
1. build the docker image
2. create the docker container

